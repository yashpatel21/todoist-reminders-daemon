services:
  todoist-reminders-daemon:
    build: .
    image: todoist-reminders-daemon:latest
    container_name: todoist-reminders-daemon
    restart: unless-stopped
    environment:
      TODOIST_API_TOKEN: ""
